const listCategory = document.querySelectorAll('.item');


const nameCategory = listCategory.lastElementChild;


console.log (`Number of categories: ${listCategory.length}`);
console.log (nameCategory);
//console.log(`Category: ${nameCategory.textContent}`);

//nameCategory.forEach(element => console.log (element));

//console.log (listCategory);

